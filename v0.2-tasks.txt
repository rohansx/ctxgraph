Create crates/ctxgraph-extract/src/temporal.rs — a 5-layer temporal date parser for ctxgraph. Layer 1: ISO-8601 (2026-03-11). Layer 2: Written dates (March 11, 2026) via regex+chrono. Layer 3: Relative dates (yesterday, 3 days ago) offset from a reference timestamp. Layer 4: Fiscal/quarter (Q3 2025, FY26) mapped to date ranges. Layer 5: Duration parsing (for 3 months). Create the crate with Cargo.toml (name=ctxgraph-extract, use workspace version/edition/license/repository), add it to the workspace members in root Cargo.toml. Export a TemporalResult enum and a parse_temporal(text, reference_time) -> Vec<TemporalResult> function. Add comprehensive tests in tests/temporal_tests.rs covering all 5 layers. Dependencies: chrono, regex. Do NOT add any ONNX/ML dependencies yet.

Create crates/ctxgraph-extract/src/model_manager.rs — model download, cache, and verification for ONNX models. Features: download model from URL to ~/.cache/ctxgraph/models/, verify SHA256 hash after download, check if model already cached, return path to cached model. Use a ModelSpec struct with {name, url, sha256, size_bytes}. Define specs for 3 models: gliner2-large-q8.onnx (~200MB), glirel-large.onnx (~150MB), minilm-l6-v2.onnx (~80MB). Use placeholder URLs for now (https://huggingface.co/ctxgraph/models/resolve/main/{name}). The download function should show progress. Dependencies: sha2, reqwest (with blocking feature), indicatif for progress bars. Add to the ctxgraph-extract crate. If the crate doesn't exist yet, create it. Add tests using tempdir for cache directory.

Create tests/fixtures/benchmark_episodes.json in the ctxgraph-extract crate — 50 manually annotated episodes for extraction quality benchmarking. Each entry should have: {text, expected_entities: [{name, entity_type, span_start, span_end}], expected_relations: [{head, relation, tail}]}. Use realistic PR descriptions, commit messages, and architecture decisions about software engineering. Cover entity types: Person, Component, Service, Language, Database, Infrastructure, Decision, Constraint, Metric. Cover relations: chose, rejected, replaced, depends_on, fixed, introduced, deprecated. Also create tests/benchmark_test.rs that loads the fixture, defines F1 scoring functions (precision, recall, f1 from predicted vs expected), and has a placeholder test that loads the JSON and validates its structure.
